# DevLog — 2026-04-05

## Session 1 — feat/core-auth Implementation

**ทำไปแล้ว:**

### Branch Setup
- `git stash` → `git checkout feat/core-auth` → `git rebase main`
- `git stash pop` — restore unstaged changes จาก main session ก่อนหน้า

### core-auth Implementation (4 ไฟล์)

**`src/app/(auth)/login/page.jsx`** — Static UI → Functional form
- `useState` (identity, password, error, loading, showPassword)
- Wire form submission กับ `signIn('credentials', { redirect: false })`
- Error banner, loading state, show/hide password toggle
- Link จริงไปยัง `/forgot-password` และ `/register`
- รับ `callbackUrl` จาก searchParams → redirect หลัง login สำเร็จ

**`src/lib/auth.js`** — RBAC helpers
- `requireRole(domain, action)` — check RBAC, throw 403 ถ้า forbidden
- `withAuth(handler, { domain, action })` — HOC สำหรับ API routes
- `normalizeRole(role)` — map legacy roles ตาม ADR-068

**`src/app/api/auth/[...nextauth]/route.js`** — Legacy role migration
- `authorize()` เรียก `normalizeRole()` บน `employee.roles[]` ก่อนเก็บลง JWT

**`src/middleware.js`** — auth guard
- `PUBLIC_PATHS`, `SKIP_PREFIXES`, callbackUrl redirect, default tenantId injection

### Commits (merged to main)
- `feat: core-auth - wire NextAuth login, RBAC helpers, legacy role mapping, middleware` (90e503b)

**Context สำคัญ:**
- `withAuth()` usage: `export const GET = withAuth(async (req, { session }) => {...}, { domain: 'customers', action: 'R' })`
- `normalizeRole()` idempotent — safe to call multiple times
- git commit บน Windows cmd ต้องใช้ `-F <file>` แทน `-m "..."` (special chars issue)

---

## Session 2 — ADR-069 + Commit Cleanup

**ทำไปแล้ว:**

### ADR-069 (branch docs/adr-069 → merged to main)
- เขียน `docs/decisions/adrs/ADR-069-ai-context-layer-notebooklm.md` ครบ 10 sections
  - 6 per-tenant notebooks + 1 platform notebook (zuri-platform)
  - Fallback: NLM timeout → direct Gemini injection
- อัปเดต `docs/HOME.md` ADR-069: Pending → Done
- Merged feat/core-auth + docs/adr-069 → main, resolved CHANGELOG.md conflict

---

## Session 3 — FEAT04-inbox Implementation

**ทำไปแล้ว:**

### Files implemented (commit 577ab92 → main)

**`GET /api/conversations`**
- Redis cache NFR2: key = `inbox:${tenantId}:v${version}:...`
- Version key `inbox:${tenantId}:version` incremented on reply/webhook → cache bust

**`GET /api/conversations/[id]`** (NEW)
- Tenantid-scoped fetch + messages (oldest→newest) + customer with insight

**`POST /api/conversations/[id]/reply`**
- Save to DB → Pusher → enqueue QStash (ไม่ call platform API โดยตรงจาก route)

**`POST /api/workers/send-message`** (NEW)
- verifyQStashSignature → call FB Messenger API หรือ LINE push API
- throw error → QStash retry (NFR3: 5 retries)

**`POST /api/webhooks/facebook`**
- respond 200 ทันที, process async (NFR1: < 200ms)
- processWebhook(): upsertFacebookCustomer → upsertConversation → appendMessage → Pusher → cache bust

**`POST /api/webhooks/line`**
- HMAC-SHA256 verify + same async pattern

**`conversationRepo`**
- เพิ่ม `getMessages(conversationId, limit)` — ordered asc
- เพิ่ม `upsertFacebookCustomer()` — upsert by facebookId (@unique)
- เพิ่ม `upsertLineCustomer()` — findFirst+create (lineId ไม่ @unique — G-WH-02)
- แก้ `appendMessage`: เอา tenantId ออก, เพิ่ม externalMessageId param

**Inbox page**
- `useSession()` → tenantId → `usePusher(`tenant-${tenantId}`)
- fetchConversations → GET /api/conversations
- handleSelectConversation → GET /api/conversations/[id] → messages + customer
- handleSend → optimistic update → POST reply → confirm/rollback
- Pusher new-message: append if current conv, refresh list

### Context สำคัญ
- Desktop Commander `start_process` แต่ละ call = fresh cmd → git branch switch ไม่ carry over → commit ลงบน main โดยตรง (ไม่ผ่าน feature branch)
- `lineId` ไม่ใช่ `@unique` ใน schema → ต้องใช้ findFirst+create, ห้ามใช้ upsert
- `appendMessage` รับ `conversationId` = internal UUID (Conversation.id), ไม่ใช่ external conversationId
- FB/LINE token ยังมาจาก env var — TODO: ย้ายไป tenant table ใน M5

---

## Session 4 — FEAT04-inbox Bug Fixes

**ทำไปแล้ว:**

### Bugs fixed จาก Session 3 (commit → main)

**`src/components/inbox/ProfileTab.jsx`**
- เพิ่ม `Sparkles` ใน import จาก `lucide-react` — ก่อนหน้านี้ใช้แต่ไม่ได้ import → ReferenceError ตอน render
- แก้ `customer.name` → `displayName` ที่ derive จาก `customer.facebookName ?? customer.lineName ?? customer.name ?? 'ลูกค้า'` — field จริงใน DB คือ `facebookName` ไม่ใช่ `name`
- แก้ `customer.id.split('-')[0]` → `customer.customerId ?? customer.id?.split('-')[0]` — ป้องกัน crash ถ้า id null

**`src/hooks/usePusher.js`**
- เปลี่ยนจาก bind handler โดยตรง → **ref pattern**: `onNewMessageRef` + `onCustomerUpdatedRef`
- เหตุผล: dep array เดิมไม่มี handler → stale closure risk ถ้า handler เปลี่ยน
- ผลลัพธ์: Pusher reconnect เฉพาะเมื่อ `channelName` เปลี่ยน แต่ handler ล่าสุดถูก call เสมอ

### Context สำคัญ
- `customer` ที่ส่งจาก inbox page → RightPanel → ProfileTab คือ raw object จาก API ซึ่งใช้ `facebookName` ไม่ใช่ `name`
- ห้ามเพิ่ม handler ใน Pusher dep array โดยตรง — ใช้ ref แทนเพื่อป้องกัน reconnect loop

---

## Session 5 — core/tenant (Multi-tenant Config Layer)

**ทำไปแล้ว:**

### Files implemented/updated

**`prisma/schema.prisma`**
- เพิ่ม `config Json @default("{}") @map("config")` ใน Tenant model
- เก็บ branding + locale ไว้ใน JSON field แทนการเพิ่ม column ทีละอัน
- ⚠️ ต้อง run `prisma db push` เพื่อ apply กับ Supabase DB

**`src/lib/repositories/tenantRepo.js`** (NEW)
- `getTenantById(id)` — cached 300s, key `tenant:id:{id}`
- `getTenantBySlug(slug)` — cached 300s, key `tenant:slug:{slug}`
- `shapeTenantConfig(tenant)` — safe public shape: `{ id, name, slug, plan, isActive, brandColor, logoUrl, vatRate, currency, timezone, hasFbPage, hasLineOa }`
- `updateTenantConfig(tenantId, updates)` — whitelist: `brandColor, logoUrl, vatRate, currency, timezone`; merge into existing config JSON; bust Redis cache (3 keys)
- `updateTenantIntegrations(tenantId, { fbPageId, lineOaId })` — สำหรับ Settings > Integrations

**`src/app/api/tenant/config/route.js`** (UPDATED)
- GET: เปลี่ยนจาก slug-based → `getTenantById(tenantId)` + `shapeTenantConfig` (ไม่ expose internal tokens)
- PATCH (NEW): `withAuth(handler, { domain: 'tenant', action: 'W' })` → `updateTenantConfig(tenantId, body)`

**`src/context/TenantContext.jsx`** (UPDATED)
- เพิ่ม `fetchConfig` เป็น stable `useCallback`
- เพิ่ม `updateConfig(updates)` — optimistic setState → PATCH /api/tenant/config → revert on failure
- Context value: `{ tenant, loading, error, refresh, updateConfig }`

**`src/app/(dashboard)/settings/page.jsx`** (UPDATED)
- Workspace section: เปลี่ยนจาก static placeholder → functional form
- School Name (read-only จาก `tenant.name`), Brand Color picker, Currency select, VAT Rate input, Timezone select
- Save calls `updateConfig(wsForm)` → success/error feedback
- `useEffect` sync form เมื่อ `tenant` load จาก context

### Context สำคัญ
- `shapeTenantConfig` ไม่ expose integration tokens — `hasFbPage`/`hasLineOa` เป็น boolean เท่านั้น (Tenant Sovereignty Rule)
- `updateTenantConfig` whitelist กัน caller overwrite arbitrary JSON keys
- Redis cache bust ต้องลบทั้ง 3 keys: `tenant:id:*`, `tenant:slug:*`, `tenant:*` (legacy key)
- การ run `prisma db push` ต้องมี DATABASE_URL ที่ถูกต้อง — Boss ต้อง run เอง

---

## Session 6 — core/crm (Customer Relationship Management)

**ทำไปแล้ว:**

### Files implemented/updated

**`src/lib/auth.js`** (UPDATED)
- เพิ่ม `normalizeRole(role)` — map legacy roles (MGR/ADM/HR → MANAGER, SLS/AGT/MKT → SALES, ...)
- เพิ่ม `withAuth(handler, { domain, action })` — HOC สำหรับ API routes ทั้งหมด
- action aliases: `'W'` → map เป็น `'F'` ใน permissionMatrix comparison

**`prisma/schema.prisma`** (UPDATED)
- Customer: เพิ่ม `name`, `tags String[]`, `assigneeId`, `mergedInto`, `deletedAt`
- `lifecycleStage` default เปลี่ยนจาก `LEAD` → `NEW` (ตาม spec)
- เพิ่ม `CustomerStageHistory` model — เก็บ transition history ทุกครั้ง
- เพิ่ม `CustomerActivity` model — unified timeline (STAGE_CHANGE, TAG_CHANGE, NOTE, etc.)
- เพิ่ม indexes: `lifecycleStage`, `deletedAt`

**`src/lib/repositories/customerRepo.js`** (REWRITE)
- `normalizePhone(raw)` — Thai E.164 normalization (`0812345678` → `+66812345678`)
- `listCustomers(tenantId, filters)` — full filters: search, stage[], tags[], channel, assigneeId, from/to + Redis cache 60s + total count
- `getCustomerById(tenantId, id)` — cached 60s, includes profile+insight+enrollments+_count
- `createCustomer(tenantId, data)` — CUST-ID generation, phone normalization
- `updateCustomer(tenantId, id, data)` — whitelist + phone normalize + cache bust
- `softDeleteCustomer(tenantId, id)` — set deletedAt (NFR-CRM-7)
- `transitionStage(tenantId, id, toStage, opts)` — `prisma.$transaction` update+history+activity (NFR5)
- `addTag / removeTag` — idempotent set ops + activity log
- `getTimeline(tenantId, id, opts)` — paginated CustomerActivity
- `mergeCustomers(tenantId, primaryId, secondaryId)` — `prisma.$transaction` move all relations (NFR-CRM-6)
- `getKpiStats(tenantId)` — total, newThisMonth, enrolled, paid — cached 60s
- `upsertByFacebookId / upsertByLineId` — for webhook use

**API routes (updated + new):**
- `GET /api/customers` — full filters + Redis + total (withAuth R)
- `POST /api/customers` — create + audit log (withAuth W)
- `GET /api/customers/[id]` — detail (withAuth R)
- `PATCH /api/customers/[id]` — update + audit + Pusher (withAuth W)
- `DELETE /api/customers/[id]` — soft delete + audit (withAuth W)
- NEW `PATCH /api/customers/[id]/stage` — stage transition (withAuth W)
- NEW `POST/DELETE /api/customers/[id]/tags` — tag add/remove (withAuth W)
- NEW `GET /api/customers/[id]/timeline` — activity timeline (withAuth R)
- NEW `POST /api/customers/[id]/merge` — merge customers (withAuth F = MANAGER+)

**`src/app/(dashboard)/crm/page.jsx`** (REWRITE)
- KPI cards (total, new this month, enrolled, paid) — real data จาก `/api/customers?kpi=1`
- Search input debounce 300ms
- Lifecycle stage filter pills (NEW/CONTACTED/INTERESTED/ENROLLED/PAID/LOST)
- Customer table: avatar, displayName, phone/email/FB badge, StageBadge, tags (max 3)
- Actions: เปิด Inbox (deep link), View detail, context menu
- New Customer modal: form create + optimistic UI update
- Pagination controls

### Context สำคัญ
- `withAuth` ใช้ action `'W'` = map เป็น `'F'` ใน can() — ต้องมี MANAGER/OWNER/SALES/DEV role
- `mergeCustomers` ใช้ action `'F'` โดยตรง = MANAGER/DEV เท่านั้น
- `lifecycleStage` default เปลี่ยนเป็น `'NEW'` — ตาม FEAT05 spec (ไม่ใช่ `'LEAD'` อีกต่อไป)
- ⚠️ ต้อง run `prisma db push` เพื่อ apply CustomerStageHistory + CustomerActivity + Customer field changes

---

## Session 7 — core/pos (Point of Sale)

**ทำไปแล้ว:**

### Files implemented/updated

**`prisma/schema.prisma`** (UPDATED)
- `Order`: เพิ่ม `tenantId String` (critical multi-tenant fix), `tableId String?`, `subtotalAmount`, `vatAmount`, `serviceCharge`, `cashReceived`, `notes`; เปลี่ยน `items Json` → relation `OrderItem[]`
- `OrderItem` (NEW model): orderId, productId, name, qty, unitPrice, vatRate, kitchenStatus (PENDING/PREPARING/READY/SERVED)
- `Transaction`: เพิ่ม `tenantId String`
- `Product`: เพิ่ม `tenantId String`, `description`, `imageUrl`, `sku`, `posPrice`, `isPosVisible`
- `PosZone` (NEW): id, tenantId, name, color, sortOrder
- `PosTable` (NEW): id, tenantId, zoneId, label, capacity, status (AVAILABLE/OCCUPIED/RESERVED/CLEANING)
- `PosReceiptConfig` (NEW): tenantId, headerText, footerText, showVat, showServiceCharge, showQr, qrPromptpayId
- Tenant: เพิ่ม `orders Order[]` relation
- ⚠️ ต้อง run `prisma db push` เพื่อ apply ทุก model ใหม่ + field เพิ่มเติม

**`src/lib/idGenerator.js`** (UPDATED)
- เพิ่ม `generateProductId()` — `generateGlobalId('product', 'productId', 'PRD', 4)`

**`src/lib/repositories/productRepo.js`** (REWRITE)
- `listProducts(tenantId, filters)` — Redis cached 120s; category, search, isActive, isPosVisible filters
- `listCategories(tenantId)` — distinct categories, cached 120s
- `createProduct(tenantId, data)` — ID gen, cache bust
- `updateProduct(tenantId, id, data)` — whitelist + cache bust
- `getPosPrice(product)` — returns `posPrice ?? basePrice`

**`src/lib/repositories/orderRepo.js`** (REWRITE)
- `calculateTotals(items, opts)` — handles VAT inclusive/exclusive, service charge, bill-level discount; VAT-inclusive formula: `vatAmount = baseForVat * vatRate / (100 + vatRate)`
- `createOrder(tenantId, data)` — orderId gen (ORD-), calculates totals, nested create OrderItems
- `listOrders(tenantId, filters)` — Redis cached 30s; status, tableId, from/to filters
- `getOrderById(tenantId, id)` — includes items (sorted by kitchenStatus), table, transactions
- `updateOrderItems(tenantId, id, data)` — `prisma.$transaction` delete all items → recreate → recalc totals
- `processPayment(tenantId, id, opts)` — `prisma.$transaction`: create Transaction + update Order→PAID + update PosTable→CLEANING
- `voidOrder(tenantId, id, opts)` — set status→VOID
- `getDailySummary(tenantId, date)` — aggregate: total orders, total revenue, by-method breakdown

**API routes (updated + new):**
- `GET /api/orders` — list orders + daily summary `?summary=1` (withAuth R)
- `POST /api/orders` — create order + audit + Pusher `new-order` (withAuth W)
- `GET /api/orders/[id]` — order detail with items + transactions (withAuth R)
- `PATCH /api/orders/[id]` — update items/discount on PENDING order (withAuth W)
- NEW `POST /api/orders/[id]/pay` — process payment + audit + Pusher `order-paid` (withAuth W)
- NEW `POST /api/orders/[id]/void` — void order + audit (withAuth F = MANAGER+)
- `GET /api/products` — list + categories shortcut `?categories=1` (withAuth R)
- `POST /api/products` — create product (withAuth F = MANAGER+)

**`src/app/(dashboard)/pos/page.jsx`** (REWRITE)
- Full-screen split layout: product browser (left, flex-1) + cart panel (right, w-80/96)
- Product grid: category pills fetched from API, search debounce 300ms, product cards with image/icon fallback, price display
- Cart panel: add/remove/qty adjust per item, bill-level discount input (฿ amount)
- Totals section: subtotal, discount, VAT 7% inclusive, **total**
- Payment modal: CASH/QR/CARD selector, cash-received + change calculator for CASH
- Full checkout flow: POST /api/orders → POST /api/orders/[id]/pay (atomic)
- Success toast 4s auto-dismiss, cart cleared on success

### Context สำคัญ
- `Order.tenantId` was missing in original schema — critical multi-tenant bug fixed; all order queries now scoped by tenantId
- `Product.tenantId` same issue — fixed
- VAT-inclusive calculation (Thai retail default): `vatAmount = subtotal * 7 / 107`, NOT `subtotal * 0.07`
- `processPayment` uses `prisma.$transaction` — Order→PAID + Transaction created + PosTable→CLEANING atomically
- OrderItem `kitchenStatus` enum allows KDS (Kitchen Display System) integration in M5+
- `getPosPrice(product)` prefers `posPrice` over `basePrice` — allows POS-specific pricing

---

## Session 8 — core/marketing (Meta Ads Analytics)

**ทำไปแล้ว:**

### Files implemented/updated

**`src/lib/repositories/marketingRepo.js`** (REWRITE)
- `parseDateRange(range)` — '7d' | '30d' | '90d' → { start, end }
- `getDashboardSummary(tenantId, range)` — KPI cards + period-over-period % change, Redis cached 300s
- `getCampaigns(tenantId, filters)` — campaign list with aggregated metrics (spend/impressions/clicks/leads/revenue/ROAS/CTR/CPL/CPC), Redis cached 300s
- `getAdSetsForCampaign(tenantId, campaignId, range)` — drill-down level 2
- `getAdsForAdSet(tenantId, adSetId, range)` — drill-down level 3
- `getTimeSeries(tenantId, range)` — daily aggregates for Recharts charts, Redis cached 300s
- `getPlacementBreakdown(tenantId, range)` — by platform + position, Redis cached 300s
- `getHourlyHeatmap(tenantId, range)` — 24×7 grid (DOW × hour), Redis cached 300s
- `clearMarketingCache(tenantId)` — clears all 7 cache key patterns after sync
- All queries scope by tenant via: `ads → ad_sets → campaigns → ad_accounts WHERE tenant_id = $tenantId`

**`src/app/api/marketing/dashboard/route.js`** (NEW)
- `GET` withAuth(marketing:R): returns getDashboardSummary — { current, previous, changes }

**`src/app/api/marketing/campaigns/route.js`** (NEW)
- `GET` withAuth(marketing:R): multi-purpose route
  - `?range=30d` — campaign list
  - `?type=timeseries` — daily time series
  - `?type=placement` — placement breakdown
  - `?type=heatmap` — hourly heatmap (24×7)
  - `?drill=adsets&campaign_id=xxx` — AdSet drill-down
  - `?drill=ads&adset_id=xxx` — Ad drill-down

**`src/app/api/workers/sync-hourly/route.js`** (REWRITE)
- Full Meta Marketing API sync (v21.0)
- Inflight Redis key guard — TTL 600s (Gotcha #2)
- QStash signature verification (NFR3)
- Loops through `ad_accounts` table to find tenants to sync
- Syncs: campaigns → adsets → ads → daily insights (last_7d overlapping window — Gotcha #7)
- Uses `$executeRaw` upsert with `ON CONFLICT` for all tables
- `throw error` at end → QStash retry (NFR5)

**`src/app/(dashboard)/marketing/page.jsx`** (REWRITE)
- KPI cards (8 cards): Spend, Revenue, ROAS, CPL, CTR, Impressions, Clicks, Leads
- Each card: period-over-period % change chip (▲/▼ with color)
- Spend vs Revenue dual-axis ComposedChart (Bar + Line, Recharts)
- CTR Trend LineChart
- Hourly Spend Heatmap (24×7 inline grid, color-coded orange scale)
- Top 10 Campaigns table (click → /marketing/campaigns)

**`src/app/(dashboard)/marketing/campaigns/page.jsx`** (REWRITE)
- Sortable campaign table: click any column header → sort asc/desc
- Search by campaign name (client-side filter)
- Status filter (All/ACTIVE/PAUSED/ARCHIVED) + range selector
- 3-level drill-down via ChevronDown/Right accordion:
  - Campaign row → click → fetches AdSets (lazy load)
  - AdSet row → click → fetches Ads (lazy load)
  - Ad row: name, status, metrics
- Metric cells shared renderer: Spend, Revenue, ROAS (color coded), CTR, Clicks, Leads, CPL

### Context สำคัญ
- Marketing tables (campaigns, ad_sets, ads) ไม่มี `tenant_id` โดยตรง — scope ผ่าน JOIN: `campaigns → ad_accounts.tenant_id`
- `ad_accounts.tenant_id` เป็น UUID type ใน DB — ต้องใช้ `::text` cast ใน `$queryRaw` template
- `getOrSet(key, fn, ttl)` signature ใน redis.js ไม่รับ redis instance — ส่งแค่ key/fn/ttl
- Drill-down lazy load ป้องกัน N+1 query — โหลดเฉพาะเมื่อ expand
- `sync-hourly` worker ยัง require `META_SYSTEM_USER_TOKEN` env var — ถ้าไม่มี token → throw gracefully

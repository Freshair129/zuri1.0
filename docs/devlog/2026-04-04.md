# DevLog — 2026-04-04

## 09:00 — Doc Restructure + RBAC + Feature Branch Setup

**ทำไปแล้ว:**
- ADR-068: Persona-based RBAC (12 roles → 7: DEV/OWNER/MANAGER/SALES/KITCHEN/FINANCE/STAFF)
- system_config.yaml v3.0.0: roles rewritten + legacy migration map
- permissionMatrix.js: rewritten for 6 persona roles + accounting domain
- SITEMAP.md v1.1.0: สร้างใหม่ 45 pages, 12 domains, role access per page
- PROJECT_MAP.md: สร้างใหม่ repo structure + domain map
- HOME.md: อัปเดต v3.0 ครบทุก link
- CLAUDE.md: RBAC + Obsidian section อัปเดต + Git workflow convention
- Domain CLAUDE.md: สร้าง 12 ไฟล์ (inbox/crm/pos/courses/schedule/kitchen/marketing/tasks/employees/settings/repositories/workers)
- Feature branches M2: feat/core-auth, feat/core-tenant, feat/FEAT04-inbox, feat/FEAT05-crm, feat/FEAT06-pos, feat/FEAT07-enrollment, feat/FEAT08-kitchen, feat/FEAT09-marketing, docs/adr-069
- Doc cleanup: ลบ/ย้ายไฟล์ซ้ำซ้อน, รวม system-overview เข้า tech-spec, ลบ site_map.md เก่า, จัด changelog/ ให้เป็นระบบ, สร้าง gotchas/incidents/

**ค้างอยู่:**
- `docs/zuri/RESTRUCTURE_PLAN.md` — ยังไม่ได้ลบ (รอ Boss confirm)
- `scripts/changelog.py` — สร้างแล้วแต่ยังไม่ได้เพิ่ม rule เข้า CLAUDE.md ให้ agent รัน
- ADR-069 (NLM Notebook Architecture) — branch สร้างแล้ว ยังไม่ได้เขียน
- PRD + feature specs อัปเดต 6 roles — Boss เอา Gemini prompt ไปทำเอง ยังไม่ confirm ว่าเสร็จ

**context สำคัญ:**
- permissionMatrix.js ใช้ 6 roles ใหม่แล้ว (ADR-068) — ถ้าแก้ code ที่เกี่ยวกับ RBAC ต้องใช้ชื่อ role ใหม่เท่านั้น
- git ต้อง run ผ่าน cmd `/D` flag: `cd /D E:\zuri && git ...` (sandbox ทำ git ตรงไม่ได้เพราะ lock file issue)
- `docs/devtools/` อยู่ใน .gitignore แล้ว (Vercel embedded repo)
- changelog/ ที่ root = active system, docs/changelog/archive/ = old versioned format

**session ถัดไปทำต่อ:**
- เริ่ม implement feat/core-auth หรือ feat/FEAT05-crm (รอ Boss ตัดสินใจว่าจะ start branch ไหน)

---

## 10:xx — Tooling Consolidation (Python migration)

**ทำไปแล้ว:**
- Merged unique sections from `.dev/agents/AGENT_PROTOCOL.md` → `CLAUDE.md`:
  - Checkpoint Protocol (ทุก 3-4 tasks + before DONE)
  - Incident Prevention Checklist (Meta API / Webhook / DB / Attribution)
  - Communication with Boss (when to ask / not ask)
- Deleted `.dev/agents/AGENT_PROTOCOL.md` (content now in CLAUDE.md)
- Created Python scripts (migrate จาก JS orchestrator):
  - `scripts/new-adr.py` — สร้าง ADR ใหม่ (ใช้ต่อจาก ADR-068)
  - `scripts/new-feature.py` — สร้าง feature spec + flow skeleton
  - `scripts/pre-commit.py` — pre-commit checks (8 rules: schema, paths, LOC, getPrisma, console.error, workers throw, tenantId)
- Deleted `.dev/orchestrator/commands/changelog.js` (superseded by scripts/changelog.py)
- Updated CLAUDE.md: เพิ่ม Scripts section (4 commands)
- **ตอบ Boss:** Python ดีกว่า JS สำหรับ agent tooling — no npm install, stdlib เพียงพอ, consistent กับ changelog.py

**ค้างอยู่:**
- ADR-069 (NLM Notebook Architecture) — branch สร้างแล้ว ยังไม่ได้เขียน
- PRD + feature specs อัปเดต 6 roles — Boss เอา Gemini prompt ไปทำเอง ยังไม่ confirm

**context สำคัญ:**
- `.dev/orchestrator/commands/` ยังมี: new-adr.js, new-feature.js, pre-commit.js, sync-check.js, verify-flow.js (JS versions — superseded แต่ยังไม่ลบ รอ Boss confirm)
- Python scripts ทำงานจาก project root: `python scripts/xxx.py`
- CLAUDE.md ตอนนี้มีทุกอย่างที่ agent ต้องรู้ รวม Checkpoint + Incident Checklist + Boss comms

**session ถัดไปทำต่อ:**
- รอ Boss ตัดสินใจว่าจะ start branch ไหน (feat/core-auth หรือ feat/FEAT05-crm)
- ถ้า Boss confirm → อ่าน spec ก่อน implement (DOC TO CODE)

---

## 14:xx — Co-dev Tooling + Orchestrator Migration

**ทำไปแล้ว:**

**IMP Handoff System:**
- ออกแบบระบบ cross-platform handoff: Claude เขียน IMP file → Antigravity execute
- สร้าง `docs/handoff/TEMPLATE.md` — format standard สำหรับ IMP files
- เพิ่ม Handoff section ใน CLAUDE.md พร้อม status flow: DRAFT → PENDING_APPROVAL → IN_PROGRESS → DONE
- สร้าง `docs/guide/co-dev-workflow.md` v1.1.0 — สรุปการทำงานทั้ง 2 workflows พร้อม Mermaid diagrams

**Co-dev Structure Cleanup:**
- สร้าง `.dev/co-dev/README.md` — commands, structure, model routing, context injection, Origin story
- สร้าง `.dev/co-dev/.gitignore` — `outputs/*` + `!outputs/.gitkeep`
- อัปเดต `.dev/co-dev/config/agents.yaml` — แก้ stale paths (RESTRUCTURE_PLAN.md → PROJECT_MAP.md, features/ → specs/, etc.)
- สร้าง `.dev/co-dev/config/prompts/migrator.md` — agent ที่ขาดหายไปจาก 9-agent roster
- อัปเดต `.dev/co-dev/config/settings.yaml` — ลบ stale context paths

**Full Orchestrator Migration (JS → Python):**
- Migrate `sync-check.js` → `scripts/sync-check.py`
- Migrate `verify-flow.js` → `scripts/verify-flow.py`
- ย้าย `.dev/orchestrator/templates/` → `.dev/templates/`
- อัปเดต template paths ใน `new-adr.py` และ `new-feature.py`
- ลบ `.dev/orchestrator/` ทั้งหมด (cli.js, commands/*.js, templates/, package.json, node_modules)
- ย้าย `agent-knowledge-graph.html` + `index.html` → `.dev/co-dev/`
- ลบ `.dev/docs/workflow.md`, `.dev/docs/co-dev.md`, `.dev/docs/` folder
- อัปเดต CLAUDE.md Scripts section — เพิ่ม sync-check.py + verify-flow.py

**Fixed bugs:**
- `scripts/changelog.py` — Python 3.10 backslash-in-f-string bug: แยก `recent_joined = "\n\n".join(...)` ออกก่อน
- ลบ `.dev/docs/co-dev.md` โดยไม่ get unique content ก่อน → restore ด้วย `git show HEAD~1:...` แล้ว merge เนื้อหา unique เข้า workflow guide → เพิ่มเป็น rule ถาวรใน CLAUDE.md แล้ว

**ค้างอยู่:**
- ADR-069 (NLM Notebook Architecture) — ยังไม่ได้เขียน
- PRD + feature specs อัปเดต 6 roles — ยังไม่ confirm จาก Boss
- Changelog entry สำหรับ orchestrator migration commit

**context สำคัญ:**
- Shared workspace: `E:\zuri` — ทั้ง Claude และ Antigravity access ได้โดยตรง ไม่ต้อง copy files
- 2 workflows: (1) Antigravity native parallel orchestration, (2) Claude Cowork + Desktop Commander → gemini CLI sequential
- Gemini auth: ใช้ Google account ของ Boss (Google One AI Premium 700 THB/เดือน) — ไม่มีค่าใช้จ่ายเพิ่ม ไม่ต้อง API key
- Python ดีกว่า JS สำหรับ tooling: no npm install, stdlib เพียงพอ
- `.dev/` structure หลัง cleanup: `co-dev/` + `templates/` + `agents/` (empty)

**session ถัดไปทำต่อ:**
- รัน `python scripts/changelog.py` สำหรับ orchestrator migration commit
- รอ Boss ตัดสินใจว่าจะ start feature branch ไหน

---

## Session ดึก — co-dev v3.1 Context Injection (Phase A/B/C Complete)

### ทำไปแล้ว

**Phase C: Tool Calling ✅**
- ตรวจงาน Antigravity Phase C → พบ `llm.py` truncated ครั้งที่ 4 (pattern เดิม: cut mid-subprocess.run)
- เขียน `llm.py` ใหม่ทั้งไฟล์: lazy imports, `call_with_tools()`, `_tool_call_anthropic()`, `_tool_call_gemini()`, renamed CLI fns
- แก้ `pipeline.py` (truncated ครั้งที่ 4): fix `call_with_tools` signature + restore duration/log/return/GateBlockedError
- แก้ `tools.py`: ADR path `docs/architecture` → `docs/decisions/adrs/`, เพิ่ม `search_entire_schema` escape hatch

**Reference Stubs ✅**
- `pos.md`: Customer stub (wallet, membershipTier, walletPoints)
- `enrollment.md`: Customer stub + Product stub (hours, sessionType)
- `marketing.md`: แก้ truncated DailyBrief closing `}` + Cross-domain FKs section + Customer stub + Conversation stub (firstTouchAdId attribution)

**RAG Index Build ✅**
- ลบ `vector.db` เก่า (768 dims, wrong) → rename `.bak` → `cli.py index` สร้างใหม่ด้วย FLOAT[1024]
- RE-INDEX COMPLETE

### Context สำคัญ

**Antigravity Truncation Pattern (L1 Gotcha)**
Truncate 4 ครั้งตลอดโปรเจกต์ — เสมอที่ช่วง LLM call ใน `pipeline.py` / `llm.py`
→ rule: ทุกครั้งที่ Antigravity เขียนไฟล์เหล่านี้ ต้อง verify `wc -l` + read tail 20 lines ก่อน claim DONE

**Phase C Signature Contract**
`call_with_tools(model, prompt, tools, tool_executor, max_rounds=5)`
- `tool_executor` = callable `(name, args) → str` → ส่ง `executor.execute` ไม่ใช่ `executor`

**bge-m3 Dims = 1024**
เปลี่ยน model ใหม่ → ต้อง delete vector.db ก่อนเสมอ (`IF NOT EXISTS` ไม่ recreate)

### ค้างอยู่ / session ถัดไป (ก่อนดึก)
- `OLLAMA_MODELS` permanent via `sysdm.cpl` (ต้อง set ทุกครั้งก่อน `ollama serve`)
- ~~อัปเดต `IMP-20260404-codev-context-injection.md` status → DONE~~ ✅
- ~~เพิ่ม Antigravity truncation rule ใน CLAUDE.md~~ ✅
- ทดสอบ pipeline จริง: `python .dev/co-dev/cli.py run "..." --agent cto`

---

## Session ดึก ต่อ — co-dev UX + Benchmark

**ทำไปแล้ว:**

### Progress Indicator (pipeline.py)
- เพิ่ม `step`, `total`, `phase_start` params ใน `_run_agent()`
- แสดง progress bar `█░` + % + ETA ก่อนแต่ละ agent
- ETA คำนวณจาก elapsed / steps_done × remaining steps
- ทุก phase runner (`_run_doc_phase`, `_run_code_phase`, `_run_migrate_phase`) ส่ง `phase_start = time.time()` + step/total

Output ตัวอย่าง:
```
  [███████░░░░░░░░░░░░░]  33%  step 2/3: cto | ETA ~56s
  [cto] -> claude-opus (Tool Mode)... done (90428ms)
```

### Benchmark Script (.dev/co-dev/scripts/benchmark.py)
- Plan A: co-dev RAG+Tools (full pipeline)
- Plan B: Plain injection (dump 6 context files → single LLM call)
- Metrics: wall time, token est., auto hallucination score (0–13)
- Hallucination: 10 BAD patterns (mongoose, gpt-4, raw SQL...) + 3 GOOD patterns (tenantId, console.error, getPrisma)
- Output: `benchmark/results/BENCH-YYYY-MM-DD-HH-MM.md` + manual rating section (Boss fills 1–5)

Usage:
```bash
# Full benchmark (A vs B)
python .dev\co-dev\scripts\benchmark.py --task "Add LINE notification to enrollment" --phase doc

# Quick: just plain injection
python .dev\co-dev\scripts\benchmark.py --task "..." --plain-only

# Quick: just co-dev
python .dev\co-dev\scripts\benchmark.py --task "..." --codev-only
```

**Context สำคัญ:**
- ยังไม่มี benchmark data จริง — ต้องรันก่อนตัดสินใจว่า RAG+Tools คุ้มหรือเปล่า
- Plain injection อาจเร็วกว่า 10x แต่อาจ hallucinate FK ที่ไม่มีใน context
- co-dev ช้ากว่าแต่ RAG pull เฉพาะ schema slice ที่เกี่ยวข้อง

**ค้างอยู่ / session ถัดไป:**
- รัน benchmark จริงสัก 2-3 task แล้ว rate ด้วยตัวเอง
- Cross-Encoder re-ranking (`retriever.py`) — ยัง pending
- `OLLAMA_MODELS` permanent env var via `sysdm.cpl`
